fastapi==0.110.1
uvicorn==0.25.0
boto3>=1.34.129
requests-oauthlib>=2.0.0
cryptography>=42.0.8
python-dotenv>=1.0.1
pymongo==4.6.3  # Track S Step 4.3 — CVE-2024-5629 fix (SSL hostname verification)
pydantic>=2.6.4
email-validator>=2.2.0
pyjwt>=2.10.1
bcrypt==4.1.3
passlib>=1.7.4
tzdata>=2024.2
motor==3.3.1
pytest>=8.0.0
# Track S Step 4.4 round 2 — pytest-asyncio needed by `asyncio_mode = auto`
# in pytest.ini. Without this plugin, async def tests fail with:
#   "async def functions are not natively supported."
pytest-asyncio>=0.23.0
# Track S Step 4.4 — coverage report generation in CI workflow
pytest-cov>=4.1.0
black>=24.1.1
isort>=5.13.2
flake8>=7.0.0
mypy>=1.8.0
python-jose>=3.3.0
requests>=2.31.0
pandas>=2.2.0
numpy>=1.26.0
python-multipart>=0.0.9
jq>=1.6.0
typer>=0.9.0
anthropic>=0.40.0
openpyxl==3.1.5
pandas==3.0.1
# Track S Step 4.4 round 3 — matplotlib mancava in requirements ma usato
# da tests/test_alert_notifications.py::TestCharts per generare PNG di
# alert charts. Localmente presente come transitive di altre deps; in
# CI fresh install fail con ModuleNotFoundError.
matplotlib>=3.8.0
# Round 4 — reportlab per PDF generation (services/order_pdf_service.py +
# test_alert_notifications.py::TestPDF + test_order_pdf_currency.py).
reportlab>=4.0.0
# Round 4 — prometheus-client per /metrics endpoint exposition (Phase 0
# Step 10). core/observability/metrics.py importa prometheus_client.
prometheus-client>=0.20.0
xlrd==2.0.2
slowapi>=0.1.9
stripe>=8.0.0
qrcode[pil]>=8.2  # E4 — ticket QR generation (Pillow for PNG output)

# Observability (Phase 1 — Step A1)
# Sentry SDK for error tracking + performance monitoring.
# Opt-in: only active if SENTRY_DSN env var is set.
sentry-sdk[fastapi]>=2.18.0

# Observability (Phase 1 — Step A3)
# Structured JSON logging with per-request context (request_id, user_id, org_id).
# Opt-in via LOG_FORMAT=json env var; if missing python-json-logger, falls
# back to text formatter with a warning.
python-json-logger>=2.0.7

# Observability (Phase 0 — Step 10)
# Prometheus exposition for metrics (counters, histograms, gauges).
# Endpoint /metrics serves text format scrapable by Prometheus / Grafana Cloud.
# Soft dependency: if missing, observability falls back to no-op (server still starts).
prometheus-client>=0.20.0
